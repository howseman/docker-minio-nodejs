{
  "name": "minio-node",
  "version": "1.0.0",
  "description": "A very simple way to emulate AWS S3 for local development",
  "main": "./src/server.js",
  "scripts": {
    "start": "node ./src/server.js",
    "dev": "nodemon -r dotenv/config ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Howseman Leal",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.884.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.7"
  }
}
